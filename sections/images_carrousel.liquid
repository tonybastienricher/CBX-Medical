<div class='cbx-container-site cbx-mt-7 cbx-mb-20 cbx-flex cbx-gap-2'>


    {%- for block in section.blocks -%}
        {%- case block.type -%}

            {%- when 'image' -%}
                {%- assign image = block.settings.image -%}
                <div class='lg:cbx-w-[537px]'>
                    <div class='cbx-rounded overflow-hidden cbx-relative lg:cbx-w-full lg:cbx-h-[713px]'>
                        {% if image %}
                            {{ image | image_url: width: 800 | image_tag: class: 'cbx-absolute cbx-object-cover cbx-inset-0 cbx-w-full cbx-h-full', loading: 'lazy' }}
                        {% else %}
                          {{ 'image' | placeholder_svg_tag: 'placeholder-svg placeholder cbx-absolute cbx-object-cover cbx-inset-0 cbx-w-full cbx-h-full' }}
                        {% endif %}
                    </div>
                    <p class='cbx-pt-2.5 cbx-text-xs cbx-font-medium cbx-uppercase cbx-text-green4 cbx-leading-tight'>
                        {{ section.settings.lgende | newline_to_br }}
                    </p>
                </div>
        {%- endcase -%}
    {%- endfor -%}
</div>

{% schema %}
{
  "name": "Images Carrousel",
  "class": "images_carrousel",
  "max_blocks": 10,
  "tag": "section",
  "settings": [],
  "blocks": [
    {
      "name": "Image",
      "type": "image",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "textarea",
          "id": "lgende",
          "label": "Légende",
          "default": "legend image and dolorem text about brands, values and behind the scene."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Images Carrousel",
      "blocks": [
        {
          "type": "image"
        },
        {
          "type": "image"
        }
      ]
    }
  ]
}
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}